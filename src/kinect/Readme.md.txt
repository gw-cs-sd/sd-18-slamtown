Requires PyKinectv2 (and anaconda 32 bit) along with other dependencies to run.

This is the api and some example code appended to the end of the file to simply grab a color frame from
kinect using PyKinectv2. It relies on opencv rather than pygame to display the image.